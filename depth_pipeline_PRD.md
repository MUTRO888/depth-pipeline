# 浮雕深度估计管线 PRD
> 供 Claude Code 实现使用

---

## 项目概述

将任意输入图片通过 AI 深度估计，自动生成适合大理石雕刻机使用的浮雕灰度图（BMP）。操作员通过图形界面上传图片、等待处理、获取输出文件，无需接触命令行。

**当前阶段范围：** 深度估计模块 + 图形界面
**输入：** JPG / PNG 图片
**输出：** 8bit 灰度 BMP，白色为高点，黑色为低点，可直接导入 JDPaint

---

## 运行环境

| 项目 | 配置 |
|------|------|
| OS | Windows 11 |
| CPU | AMD Ryzen 5 5600H |
| RAM | 16GB |
| GPU | NVIDIA RTX 3050 Laptop，专用显存 4GB |
| CUDA 驱动 | 581.95（CUDA 12.x） |
| Python | 3.10+ |
| 开发机 | Mac（仅写代码，不运行模型） |

---

## 用户角色

**操作员：** 工厂内部使用，非技术背景，只需要会拖图片和点按钮。

---

## 界面结构

整个应用为单窗口，从上到下分为三个区域：

```
┌─────────────────────────────┐
│        上传区域              │
├─────────────────────────────┤
│        处理状态区域           │
├─────────────────────────────┤
│        导出区域              │
└─────────────────────────────┘
```

---

## 功能模块

### 1. 上传区域

**触发方式：**
- 点击区域，弹出系统文件选择器，限制只能选择 JPG / PNG
- 拖拽图片文件到区域内

**上传后行为：**
- 区域内显示图片缩略图预览
- 显示文件名和尺寸信息（宽 × 高，单位 px）
- 出现"开始处理"按钮
- 若已有上次的处理结果，导出区域重置为空

**错误处理：**
- 上传非图片文件：提示"请上传 JPG 或 PNG 格式的图片"
- 上传文件损坏无法读取：提示"文件读取失败，请重新选择"

---

### 2. 处理状态区域

**初始状态：**
区域为空，不显示任何内容。

**点击"开始处理"后，按顺序展示以下状态：**

| 阶段 | 显示文字 |
|------|---------|
| 启动 | 正在加载模型... |
| 深度估计进行中 | 正在分析图片深度，请稍候 |
| 后处理 | 正在生成灰度图... |
| 完成 | 处理完成 |

**进度表达：**
- 使用进度条，在"正在分析图片深度"阶段展示（该阶段耗时最长）
- 进度条为不确定进度样式（循环动画），不显示百分比，因为无法精确预估时间

**处理中的交互限制：**
- 上传区域禁用，不可重新上传
- "开始处理"按钮变为灰色不可点击
- 处理完成后解除所有限制

**错误处理：**
- 显存不足：提示"显存不足，已自动降低处理精度重试"，继续处理不中断
- 其他未知错误：提示"处理失败，请重试"，并显示错误简述，恢复上传和按钮状态

---

### 3. 导出区域

**处理完成前：** 区域为空，不显示任何内容。

**处理完成后，显示：**
- 灰度图预览（与原图并排对比，左侧原图，右侧灰度图）
- 默认导出路径（与输入图片同目录，文件名为原文件名加 `_depth` 后缀，格式 BMP）
- 可修改导出路径的输入框 + 旁边的"浏览"按钮，点击弹出系统目录选择器
- "保存文件"按钮

**点击"保存文件"后：**
- 文件写入指定路径
- 按钮变为"已保存 ✓"，持续 2 秒后恢复为"保存文件"
- 若目标路径已有同名文件，弹出确认框"文件已存在，是否覆盖？"

**错误处理：**
- 目标路径无写入权限：提示"无法写入该目录，请选择其他位置"
- 磁盘空间不足：提示"磁盘空间不足"

---

## 完整操作流程

```
启动应用
    │
    ▼
上传图片（点击或拖拽）
    │
    ▼
预览图片，确认无误
    │
    ▼
点击"开始处理"
    │
    ▼
等待处理（查看进度状态）
    │
    ├─ 出错 → 提示错误，可重试
    │
    ▼
查看原图 / 灰度图对比预览
    │
    ▼
确认或修改导出路径
    │
    ▼
点击"保存文件"
    │
    ▼
文件就绪，导入 JDPaint
```

---

## 深度估计模块

### 模型

| 项目 | 值 |
|------|---|
| 模型 | Marigold v1.1（`prs-eth/marigold-depth-v1-1`） |
| 协议 | Apache 2.0（代码）+ RAIL++-M（权重） |
| 商业使用 | 允许，内部工厂使用无限制 |
| 权重大小 | 约 1.5GB |
| 首次运行 | 自动下载，缓存后离线可用 |

### 核心参数

| 参数 | 值 | 说明 |
|------|---|------|
| torch_dtype | float16 | 4GB 显存必须用 fp16 |
| ensemble_size | 5 | 4GB 显存的上限，影响稳定性 |
| denoising_steps | 10 | 拉满，对显存影响小 |
| device | cuda | 固定使用 GPU |

### 后处理逻辑

1. 归一化深度值到 0–255
2. 反转深度方向（白色为高点，符合 JDPaint 导入规范）
3. 高斯平滑（半径 2），消除噪点，使雕刻面更平整
4. 输出 8bit 灰度 BMP

### 显存不足兜底

若处理过程中触发 OOM，自动降级参数（ensemble_size 降为 1），清空显存缓存后重试，不中断流程，界面给出提示。

---

## 输出文件规格

| 参数 | 值 |
|------|---|
| 格式 | BMP |
| 色彩模式 | 灰度 8bit，单通道 |
| 高点 | 白色（255） |
| 低点 | 黑色（0） |
| 分辨率 | 与输入图一致 |

---

## 技术选型（GUI 框架）

使用 **Tkinter**（Python 内置，无需额外安装）实现图形界面，保证在 Windows 上开箱即用，打包方便。

---

## 模型下载

- 首次运行自动从 Hugging Face 下载，缓存到 `C:\Users\<用户名>\.cache\huggingface\hub`
- 网络受限时：手动下载后，将配置中的模型路径改为本地目录

---

## 目录结构

```
depth_pipeline/
├── app.py                   # GUI 入口
├── pipeline.py              # 深度估计主流程
├── steps/
│   └── depth_estimation.py  # 深度估计模块
├── utils/
│   ├── image_io.py          # 图片读写
│   └── postprocess.py       # 后处理
├── config.yaml              # 参数配置
├── requirements.txt         # 依赖
└── README.md                # 使用说明
```

---

## 后续扩展（不在当前范围）

- `steps/super_resolution.py`：超分辨率（Real-ESRGAN）
- `steps/segmentation.py`：主体分割（SAM / rembg）
- `steps/relighting.py`：光照均一化（IC-Light）
- 批量处理：支持一次上传多张图片
